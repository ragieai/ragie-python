"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from ragie.types import BaseModel, Nullable, UNSET_SENTINEL
from ragie.utils import validate_const
from typing import Any, Dict, Literal
from typing_extensions import Annotated, TypedDict


class DocumentUpdateWebhookPayloadTypedDict(TypedDict):
    document_id: str
    partition: str
    metadata: Dict[str, Any]
    external_id: Nullable[str]
    name: str
    connection_id: Nullable[str]
    sync_id: Nullable[str]
    error: Nullable[str]
    status: Literal["ready"]


class DocumentUpdateWebhookPayload(BaseModel):
    document_id: str

    partition: str

    metadata: Dict[str, Any]

    external_id: Nullable[str]

    name: str

    connection_id: Nullable[str]

    sync_id: Nullable[str]

    error: Nullable[str]

    STATUS: Annotated[
        Annotated[Literal["ready"], AfterValidator(validate_const("ready"))],
        pydantic.Field(alias="status"),
    ] = "ready"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = []
        nullable_fields = ["external_id", "connection_id", "sync_id", "error"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m

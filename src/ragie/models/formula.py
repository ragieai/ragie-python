"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from ragie.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from ragie.utils import validate_const
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class FormulaTypedDict(TypedDict):
    content: str
    r"""The formula as plain text."""
    type: Literal["Formula"]
    latex: NotRequired[Nullable[str]]
    r"""The LaTeX representation of the formula."""


class Formula(BaseModel):
    content: str
    r"""The formula as plain text."""

    TYPE: Annotated[
        Annotated[
            Optional[Literal["Formula"]], AfterValidator(validate_const("Formula"))
        ],
        pydantic.Field(alias="type"),
    ] = "Formula"

    latex: OptionalNullable[str] = UNSET
    r"""The LaTeX representation of the formula."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["type", "latex"])
        nullable_fields = set(["latex"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    Formula.model_rebuild()
except NameError:
    pass

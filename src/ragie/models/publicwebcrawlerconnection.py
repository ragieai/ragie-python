"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .webcrawlerdata import WebcrawlerData, WebcrawlerDataTypedDict
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from ragie.types import BaseModel, UNSET_SENTINEL
from ragie.utils import validate_const
from typing import Any, Dict, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class PublicWebcrawlerConnectionTypedDict(TypedDict):
    data: WebcrawlerDataTypedDict
    provider: Literal["webcrawler"]
    credentials: NotRequired[Dict[str, Any]]


class PublicWebcrawlerConnection(BaseModel):
    data: WebcrawlerData

    PROVIDER: Annotated[
        Annotated[Literal["webcrawler"], AfterValidator(validate_const("webcrawler"))],
        pydantic.Field(alias="provider"),
    ] = "webcrawler"

    credentials: Optional[Dict[str, Any]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["credentials"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    PublicWebcrawlerConnection.model_rebuild()
except NameError:
    pass
